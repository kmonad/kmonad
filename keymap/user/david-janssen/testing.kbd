
;; Basic system config
(defcfg
  input  (device-file "/dev/input/by-id/usb-04d9_daskeyboard-event-kbd")
  output (uinput-sink "KMonad Keyboard" "hello" (500,1))
  fallthrough true
  )

(defsrc a s d f)

(deflayer layer1
  (around-next-timeout 500 lsft q)
  (tap-hold 500 lsft q)
  y
  z
)
